# URL of the FastAPI backend. For local development this is the default
# docker-compose api service. For production, we must replace with our Render URL.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
